name: claude
description: Claude Code agent
handler: exec
backend:
  command: claude
  args: ["-p", "--output-format", "json"]
  session_flag: "--session-id"
skills:
  - id: code
    name: Code Assistant
  - id: general
    name: General
